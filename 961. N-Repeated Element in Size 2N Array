class Solution {
public:
    int repeatedNTimes(vector<int>& nums) {
        unordered_map<int,int>m;
        for(int x:nums){
            m[x]++;
        }
        int n=nums.size()/2;
        for(auto x:m){
            if(x.second==n) return x.first;
        }
        return -1;
    }
};
