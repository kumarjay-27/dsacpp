class Solution {
public:
    int furthestDistanceFromOrigin(string moves) {
        int r=0,l=0,d=0;
        for(char ch:moves){
            if(ch=='L') l++;
            else if(ch=='R') r++;
            else d++;
        }
        return abs(l-r)+d;
    }
};
