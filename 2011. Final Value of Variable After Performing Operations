class Solution {
public:
    int finalValueAfterOperations(vector<string>& operations) {
        int x=0;
        for(auto a:operations){
            if(a=="++X"||a=="X++") x=x+1;
            else x=x-1;
        }
        return x;
    }
};
