class Solution {
public:
int m = 1e9+7;
    int concatenatedBinary(int n) {
        long res=0;
        for(int i=1;i<=n;i++){
            int digits=log2(i)+1;
            res=((res<<digits)%m +i)%m;
        }
        return res;
    }
};
