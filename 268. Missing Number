class Solution {
public:
    int missingNumber(vector<int>& nums) {
       int n= nums.size();
       int sum1= (n*(n+1))/2;
       int sums=0;
       for(int i=0;i<nums.size();i++)
       {
        sums=sums+nums[i];
       }
       return (sum1-sums);
    }

};
